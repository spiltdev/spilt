---
title: TypeScript SDK
order: 1
description: Type-safe client for interacting with Pura contracts via viem
---

The `@puraxyz/sdk` package provides a TypeScript client for interacting with all 32 Pura [smart contracts](/docs/contracts).

## Installation

```bash
npm install @puraxyz/sdk
```

## Features

- Contract ABIs: pre-compiled ABIs for all 32 contracts
- Type-safe interactions: full TypeScript types via [viem](https://viem.sh/)
- 23 action modules: Core BPE + Demurrage + Relay + Lightning + Platform + V2 Economy + Thermodynamic
- Address registry: deployed contract addresses for supported networks
- EIP-712 signing: helpers for attestations and completion receipts

## Usage

```typescript
import { addresses, contracts } from '@puraxyz/sdk'

// Get contract addresses for Base Sepolia
const { capacityRegistry, backpressurePool, stakeManager } = addresses.baseSepolia

// Use with viem
import { createPublicClient, http } from 'viem'
import { baseSepolia } from 'viem/chains'

const client = createPublicClient({
  chain: baseSepolia,
  transport: http(),
})

// Read smoothed capacity for a sink
const capacity = await client.readContract({
  ...contracts.capacityRegistry,
  functionName: 'getSmoothedCapacity',
  args: [taskTypeId, sinkAddress],
})
```

## Action Modules

### Core BPE

| Module | Description |
|--------|-------------|
| [`actions/sink`](https://github.com/puraxyz/puraxyz/blob/main/sdk/src/actions/sink.ts) | Register/deregister sinks |
| [`actions/source`](https://github.com/puraxyz/puraxyz/blob/main/sdk/src/actions/source.ts) | Register task types, start/stop payment streams |
| [`actions/pool`](https://github.com/puraxyz/puraxyz/blob/main/sdk/src/actions/pool.ts) | Create pools, rebalance, read member units |
| [`actions/stake`](https://github.com/puraxyz/puraxyz/blob/main/sdk/src/actions/stake.ts) | Stake/unstake tokens, read capacity cap |
| [`actions/buffer`](https://github.com/puraxyz/puraxyz/blob/main/sdk/src/actions/buffer.ts) | Deposit to / drain from escrow buffer |
| [`actions/pricing`](https://github.com/puraxyz/puraxyz/blob/main/sdk/src/actions/pricing.ts) | Report queue load, advance pricing epoch, read prices |
| [`actions/completion`](https://github.com/puraxyz/puraxyz/blob/main/sdk/src/actions/completion.ts) | Record completions, advance completion epoch |
| [`actions/aggregator`](https://github.com/puraxyz/puraxyz/blob/main/sdk/src/actions/aggregator.ts) | Submit batched off-chain attestations |
| [`signing`](https://github.com/puraxyz/puraxyz/blob/main/sdk/src/signing.ts) | EIP-712 signing helpers |

### Domain Extensions

| Module | Description |
|--------|-------------|
| [`actions/demurrage`](https://github.com/puraxyz/puraxyz/blob/main/sdk/src/actions/demurrage.ts) | Wrap/unwrap demurrage tokens, rebase, read decay rates |
| [`actions/relay`](https://github.com/puraxyz/puraxyz/blob/main/sdk/src/actions/relay.ts) | Register Nostr relays, join pools, set anti-spam minimums |
| [`actions/lightning`](https://github.com/puraxyz/puraxyz/blob/main/sdk/src/actions/lightning.ts) | Register Lightning nodes, join routing pools, get optimal routes |
| [`actions/platform`](https://github.com/puraxyz/puraxyz/blob/main/sdk/src/actions/platform.ts) | Aggregate reputation, stake discounts, route attestations |

### V2 Economy

| Module | Description |
|--------|-------------|
| [`actions/economy`](https://github.com/puraxyz/puraxyz/blob/main/sdk/src/actions/economy.ts) | Deploy economies via EconomyFactory, query deployments |
| [`actions/nestedPool`](https://github.com/puraxyz/puraxyz/blob/main/sdk/src/actions/nestedPool.ts) | Register child pools, hierarchical rebalance, effective capacity |
| [`actions/quality`](https://github.com/puraxyz/puraxyz/blob/main/sdk/src/actions/quality.ts) | Report latency/errors/satisfaction, compute quality scores |
| [`actions/velocityToken`](https://github.com/puraxyz/puraxyz/blob/main/sdk/src/actions/velocityToken.ts) | Wrap/unwrap with idle decay, stream exemption checks |
| [`actions/urgencyToken`](https://github.com/puraxyz/puraxyz/blob/main/sdk/src/actions/urgencyToken.ts) | TTL-stamped deposits, consume, burn, batch operations |

### Thermodynamic & Monitoring

| Module | Description |
|--------|-------------|
| [`actions/temperature`](https://github.com/puraxyz/puraxyz/blob/main/sdk/src/actions/temperature.ts) | Read system temperature from capacity variance |
| [`actions/virial`](https://github.com/puraxyz/puraxyz/blob/main/sdk/src/actions/virial.ts) | Virial ratio monitoring, capital equilibrium |
| [`actions/systemState`](https://github.com/puraxyz/puraxyz/blob/main/sdk/src/actions/systemState.ts) | Aggregate system snapshots (temperature, virial, pressure) |
| [`actions/verify`](https://github.com/puraxyz/puraxyz/blob/main/sdk/src/actions/verify.ts) | On-chain verification helpers |
| [`actions/openclaw`](https://github.com/puraxyz/puraxyz/blob/main/sdk/src/actions/openclaw.ts) | OpenClaw agent registration and reputation |

### High-level client

The SDK also exports a high-level client with three convenience functions that wrap the lower-level action modules:

```typescript
import { route, deploy, monitor } from '@puraxyz/sdk'

// Route an inference request through the gateway
const result = await route({ apiKey: 'pura_...', messages: [...] })

// Register as a provider (stake + register + connect to pool)
const txs = await deploy(wallet, addresses, { taskTypeId, initialCapacity: 100n, stakeAmount: 400n * 10n ** 18n })

// Read live protocol state
const state = await monitor(client, addresses)
```

## Source

SDK source code is on [GitHub](https://github.com/puraxyz/puraxyz/tree/main/sdk/src).

---

## See also

- [Smart Contracts](/docs/contracts): all 32 deployed contracts with source
- [EconomyFactory](/docs/economy-factory): one-transaction economy deployment
- [Simulation](/docs/simulation): agent-based validation of BPE claims
- [NIP-XX](/docs/nip-xx): Nostr relay economics specification
- [Get started](/docs/getting-started): deploy an economy in 5 minutes
